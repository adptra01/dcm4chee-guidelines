# integration-core

Package **integration-core** — placeholder (skeleton MS0).

- Implementasi dimulai per milestone (MS1: dicom-core).
- Instal: `pip install -e packages/integration-core`
- Aturan: package tidak boleh bergantung pada `products/*` (ADR-002).
