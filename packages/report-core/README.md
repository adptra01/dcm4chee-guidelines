# report-core

Package **report-core** — placeholder (skeleton MS0).

- Implementasi dimulai per milestone (MS1: dicom-core).
- Instal: `pip install -e packages/report-core`
- Aturan: package tidak boleh bergantung pada `products/*` (ADR-002).
