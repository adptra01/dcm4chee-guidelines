[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dicom-core"
version = "0.1.0"
description = "Inti DICOM: parser, metadata, preview, validate, echo/store (SCU)"
requires-python = ">=3.11"
dependencies = [
    "pydicom>=3.0",
    "pynetdicom>=3.0",
    "numpy>=1.26",
    "pillow>=10.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
